//import Navbar from "../components/navbar"

export default function Apply(){
    throw new Error("Error")
    return(
        <div>
            {/*<Navbar/ > */}
            <h1>Apply Page</h1> 
            </div> ) //due to Error
}